# 小姐牌 · ZH JOKER TABLE V2

GitHub Pages 静态网页版本。

## 使用

将本目录内的 `index.html`、`README.md` 和 `assets` 文件夹一起覆盖到 GitHub 仓库 `zh-joker-table` 根目录，然后在 GitHub Desktop 中：

1. Commit to main
2. Push origin
3. GitHub Pages 会自动重新部署

不要只上传 `index.html`，`assets` 文件夹必须一起上传。
